[Unit]
Description=Biomapper UI Express API Server
After=network.target

[Service]
Type=simple
User=ubuntu
Group=ubuntu
WorkingDirectory=$DEPLOY_DIR/artifacts/api-server

# Environment variables
Environment="PATH=/usr/local/bin:/usr/bin:/bin"
Environment=PORT=8080
Environment=NODE_ENV=production
EnvironmentFile=$DEPLOY_DIR/.env

ExecStart=/usr/bin/node --enable-source-maps $DEPLOY_DIR/artifacts/api-server/dist/index.mjs

# Restart policy
Restart=always
RestartSec=10

# Logging
StandardOutput=journal
StandardError=journal
SyslogIdentifier=biomapper-ui-express

# Security hardening
NoNewPrivileges=true
PrivateTmp=true
ProtectSystem=strict
ProtectHome=read-only
ReadWritePaths=$DEPLOY_DIR

[Install]
WantedBy=multi-user.target
